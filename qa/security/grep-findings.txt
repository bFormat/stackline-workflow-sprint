# Risky JS sinks in shipped source
src/render.ts:185:    if (title.innerHTML !== wantHtml) title.innerHTML = wantHtml;

# Secret-shaped strings in repo (excluding lockfile and .git)
./qa/security/scan.sh:19:echo "[security-qa] grep for risky sinks / hardcoded secrets"
./qa/security/scan.sh:28:    '(secret|password|api[_-]?key|BEGIN[[:space:]]+(RSA|EC|DSA|OPENSSH|PRIVATE)|AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{20,})' \
